import { EmployeeCapabilityPipe } from './employee-capability.pipe';

describe('EmployeeCapabilityPipe', () => {
  it('create an instance', () => {
    const pipe = new EmployeeCapabilityPipe();
    expect(pipe).toBeTruthy();
  });
});
