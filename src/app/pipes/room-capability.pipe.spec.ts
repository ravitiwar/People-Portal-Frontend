import { RoomCapabilityPipe } from './room-capability.pipe';

describe('RoomCapabilityPipe', () => {
  it('create an instance', () => {
    const pipe = new RoomCapabilityPipe();
    expect(pipe).toBeTruthy();
  });
});
